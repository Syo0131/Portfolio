---
interface Props {
  class?: string;
  titleKey?: string;
}

const { class: className = "", titleKey } = Astro.props;
---

<h2
  class={`text-2xl sm:text-3xl md:text-4xl font-bold text-center mb-8 sm:mb-12 md:mb-16 text-shakespeare-200 ${className}`}
  data-i18n={titleKey}
>
  <slot />
</h2>
