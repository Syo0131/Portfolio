---
import SectionTitle from "../SectionTitle.astro";
import { Icon } from "astro-icon/components";
---

<section
  id="contacto"
  class="py-12 sm:py-16 md:py-24 px-4 sm:px-6 lg:px-8"
  aria-labelledby="contact-title"
>
  <div class="max-w-2xl mx-auto">
    <SectionTitle class="mb-6 sm:mb-8 md:mb-10">
      ¿Tienes algún proyecto en mente?
    </SectionTitle>

    <div
      class="bg-slate-900/80 backdrop-blur-sm border border-shakespeare-800/50 rounded-2xl p-6 sm:p-8 md:p-10 shadow-xl"
    >
      <p class="text-base text-slate-200 mb-6 sm:mb-8 text-center">
        Completa el formulario y te responderé lo antes posible.
      </p>

      <form id="contact-form" class="space-y-4 sm:space-y-6">
        <div style="display: none;" aria-hidden="true">
          <input type="text" name="website" tabindex="-1" autocomplete="off" />
        </div>

        <div>
          <label for="name" class="block text-sm font-bold text-slate-100 mb-2">
            Nombre
          </label>
          <input
            type="text"
            id="name"
            name="from_name"
            required
            minlength="2"
            maxlength="100"
            class="w-full px-4 py-3 bg-slate-950 border border-shakespeare-800/50 rounded-lg text-white placeholder-slate-400 focus:ring-2 focus:ring-shakespeare-500/50 outline-none"
            placeholder="Tu nombre"
          />
        </div>

        <div>
          <label
            for="email"
            class="block text-sm font-bold text-slate-100 mb-2"
          >
            Email
          </label>
          <input
            type="email"
            id="email"
            name="reply_to"
            required
            class="w-full px-4 py-3 bg-slate-950 border border-shakespeare-800/50 rounded-lg text-white placeholder-slate-400 focus:ring-2 focus:ring-shakespeare-500/50 outline-none"
            placeholder="tu@email.com"
          />
        </div>

        <div>
          <label
            for="message"
            class="block text-sm font-bold text-slate-100 mb-2"
          >
            Mensaje
          </label>
          <textarea
            id="message"
            name="message"
            required
            minlength="10"
            maxlength="1000"
            rows="5"
            class="w-full px-4 py-3 bg-slate-950 border border-shakespeare-800/50 rounded-lg text-white placeholder-slate-400 focus:ring-2 focus:ring-shakespeare-500/50 outline-none resize-none"
            placeholder="Cuéntame sobre tu proyecto..."></textarea>
        </div>

        <button
          type="submit"
          id="submit-btn"
          aria-live="polite"
          class="group w-full bg-shakespeare-600 text-white font-bold py-3 sm:py-4 px-6 sm:px-8 rounded-full hover:bg-shakespeare-500 transition-all flex items-center justify-center gap-2 disabled:opacity-50"
        >
          <Icon
            name="material-symbols:mail-rounded"
            class="w-5 h-5 sm:w-6 sm:h-6"
            aria-hidden="true"
          />
          <span id="btn-text">Enviar mensaje</span>
        </button>

        <p
          id="status-message"
          class="text-center text-sm font-medium min-h-[20px]"
          aria-live="assertive"
        >
        </p>
      </form>
    </div>
  </div>
</section>
